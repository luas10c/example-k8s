/** @type{import('prettier').Config} */
const config = {
  singleQuote: true,
  bracketSpacing: true,
  arrowParens: 'always',
  trailingComma: 'none',
  printWidth: 90,
  tabWidth: 2,
  semi: false
}

export default config
